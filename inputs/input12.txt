int fib(int n) {
    if (n <= 1) { 
        return n;
    }
    return fib(n-1) + fib(n-2);
}

int main() {
    int n;
    int i;

    n = 2;
    i = 0;

    while (i < n) {
        int fibo;
        fibo = fib(i); 
        printf(fibo);
        i = i + 1;
    }

    return 0;
}
